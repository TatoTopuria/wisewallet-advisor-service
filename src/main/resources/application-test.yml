spring:
  datasource:
    url: jdbc:tc:postgresql:16:///wisewallet_advisor
    username: test
    password: test
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:9092}
  data:
    redis:
      host: localhost
      port: 6379
  liquibase:
    enabled: true

wisewallet:
  advisor:
    enabled: true
